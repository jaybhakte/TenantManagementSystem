<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vacancy list</title>
    <style>
        td,tr{
            text-align: center;
            margin-top: 10px;
        }
    </style>
    <!-- Bootstrap CSS & icon file-->
    {{>iconImage}}
</head>

<body>
    <div class="container">
        {{>navbar}}

            <table class="mt-3  table-bordered table table-striped table-dark">
                <thead>
                    <tr>
                        <th scope="col">Room Number</th>
                        <th scope="col">Room Capacity</th>
                        <th scope="col">Vacancy</th>
                        <th scope="col">Image</th>
                        <th scope="col">Update</th>
                        <th scope="col">Delete</th>
                    </tr>
                </thead>

                <tbody>
                    {{#each imagelist}}
                    <tr>
                        <td scope="row"> {{this.room_number}} </td>
                        <td scope="row">{{this.room_capacity}}</td>
                        <td scope="row">{{this.vacancy}}</td>
                        <td scope="row"><img class="img img-fluid" src="/vacantrooms/{{this.image.data}}" alt="" style="height: 100px;width:150px"></td>
                        <td><a class="btn btn-success" href="/updatevimage/{{this._id}}">Update</a></td> 
                        <td><a class="btn btn-danger" href="/deletevimage/{{this._id}}">Delete</a></td> 

                    </tr>
                    {{/each}}
                </tbody>
            </table>

        {{>footer}}
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
        crossorigin="anonymous"></script>
</body>

</html>