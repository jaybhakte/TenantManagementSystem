<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Roomys</title>
    <!-- Bootstrap CSS & icon file-->
    {{>iconImage}}
</head>

<body>
    <div class="container">
        {{>navbar}}

            <table class="mt-3  table-bordered table table-striped table-dark">
                <thead>
                    <tr>
                        <th scope="col">Name</th>
                        <th scope="col">Phone</th>
                        <th scope="col">Age</th>
                        <th scope="col">Register Date</th>
                        <th scope="col">Gender</th>
                        <th scope="col">Address</th>
                        <th scope="col">Update</th>
                        <th scope="col">Delete</th>
                    </tr>
                </thead>

                <tbody>
                    {{#each list}}
                    <tr>
                        <td scope="row">{{this.name}}</td>
                        <td>{{this.phone}}</td>
                        <td>{{this.age}}</td>
                        <td>{{this.registerDate}}</td>
                        <td>{{this.gender}}</td>
                        <td>{{this.address}}</td>
                        <td><a class="btn btn-success" href="/update/{{this._id}}">Update</a></td>
                        <td><a class="btn btn-danger" href="/delete/{{this._id}}">Delete</a></td>

                    </tr>
                    {{/each}}
                </tbody>
            </table>

        {{>footer}}
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
        crossorigin="anonymous"></script>
</body>

</html>